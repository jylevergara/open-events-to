{
  "name": "open-events-to",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=16.x"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
